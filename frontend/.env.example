# 前端环境变量配置示例
# 复制此文件为 .env 或 .env.local 并根据实际情况修改

# API 基础地址
# 在 Docker 环境中使用相对路径（通过 vite 代理）
# 在本地开发时也可以使用相对路径（通过 vite 代理），或使用完整 URL
VITE_API_BASE_URL=/api/v1

# 应用标题
VITE_APP_TITLE=保卫核心业务智能体

# Docker 环境标识（Docker 环境中会自动设置为 true）
# DOCKER_ENV=true

# Node 环境
# NODE_ENV=development
